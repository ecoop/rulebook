# Rulebook

_Last updated: 2026-07-21_

A small RAG (retrieval-augmented generation) prototype that answers questions about the rules of **ultimate** and **goaltimate** — two disc sports with overlapping but distinct rulebooks. Ask a question, get an answer with citations back to the specific rule numbers, and see the underlying retrieved chunks that produced the answer.

This project exists as much to **learn RAG hands-on** as to be a working tool. The code is organized so each stage of the pipeline lives in its own small module with comments explaining the _why_, not just the _what_.

## What's in here

```
rulebook/
├── rules/                        # source materials, one subdir per sport
│   ├── ultimate/                 #   sport is derived from the directory name
│   └── goaltimate/               #   add a sport by creating a new sibling dir
├── data/index/                   # local vector index (regenerated by build_index.py)
├── src/rulebook/
│   ├── config.py                 # env loading + model selection in one place
│   ├── ingest.py                 # PDF → structured text with page numbers
│   ├── chunking.py               # section-aware chunker (splits on rule numbering)
│   ├── embeddings.py             # pluggable Voyage / OpenAI interface
│   ├── store.py                  # in-memory numpy vector store (persisted as .npy + .jsonl)
│   ├── retrieve.py               # single-sport and cross-sport retrieval
│   ├── generate.py               # Claude call with citation-forcing prompt
│   └── pipeline.py               # end-to-end: question → answer + citations
├── scripts/build_index.py        # one-shot: ingest both PDFs → LanceDB
├── api/main.py                   # FastAPI: POST /ask
├── web/                          # Vite + React + Tailwind frontend
└── notebooks/
    └── 01_explore_retrieval.ipynb  # poke the retriever, see what it returns
```

## How it works, briefly

1. **Ingest** — pull raw text out of each PDF, keeping page numbers.
2. **Chunk** — split into semantically meaningful pieces. For rulebooks, that means honoring section boundaries (`1.A.1`, `2.B.2.a`) instead of chopping mid-clause.
3. **Embed** — turn each chunk into a fixed-length vector via Voyage AI so that semantically similar chunks land near each other.
4. **Store** — persist vectors + metadata to `data/index/` (a `.npy` matrix plus a `.jsonl` of chunk records). At query time we load everything into memory; for a few hundred chunks a numpy dot product IS the vector search.
5. **Retrieve** — at query time, embed the question and pull the top-_k_ nearest chunks. For comparison queries ("does ultimate allow X, does goaltimate?"), retrieve top-_k_ per sport.
6. **Generate** — hand the retrieved chunks to Claude with a prompt that forces answers to cite the source rule id, and refuse to invent rules that aren't in context.

The FastAPI response includes the retrieved chunks alongside the answer so the frontend can show you exactly what the model was given. Watching retrieval quality drive answer quality is the whole point.

## Setup

You need [uv](https://docs.astral.sh/uv/) installed.

```bash
# 1. Install deps into a local .venv
uv sync

# 2. Copy the env template and add your keys
cp .env.example .env
$EDITOR .env
```

You'll need:

- `ANTHROPIC_API_KEY` — for the generation step. **Claude Max covers the claude.ai chat product, not API usage** — this key is billed separately, per token. For this project the total cost is pennies per session.
- `VOYAGE_API_KEY` — for embeddings. Sign up at [voyageai.com](https://www.voyageai.com/). One-time cost to build the index over both rulebooks is well under a cent. You can swap to OpenAI by setting `EMBEDDING_PROVIDER=openai` and adding `OPENAI_API_KEY`.

## Build the index

```bash
uv run python scripts/build_index.py
```

This ingests both rulebooks under `rules/`, chunks them, embeds each chunk, and writes the result to `data/index/`. Idempotent — re-running rebuilds from scratch.

## Ask questions

Three ways, in order of increasing polish:

**From the notebook** (best for learning):

```bash
uv run jupyter lab notebooks/01_explore_retrieval.ipynb
```

**From the API:**

```bash
uv run uvicorn api.main:app --reload
# then, in another shell:
curl -X POST http://localhost:8000/ask \
  -H 'content-type: application/json' \
  -d '{"question": "is double-teaming allowed?"}'
```

**From the frontend:**

```bash
cd web && npm install && npm run dev
# open http://localhost:5173
```

## Extending to other sports

Adding soccer or basketball is a matter of:

1. Drop the rulebook PDF into `rules/<sport>/` (create the directory if the sport is new).
2. Add its metadata (name, filename) to the `SOURCES` list in `scripts/build_index.py`.
3. Rebuild the index.

Nothing in the retrieval or generation layers is sport-specific — they read the `sport` field off the retrieved chunk.

## Invite-gated demo mode (optional)

For pre-production or invite-only previews, Rulebook can gate every request behind a per-recipient invite token. It uses the [`guest-auth`](https://github.com/ecoop/guest-auth) library — the same middleware Pitchcraft runs.

**Off by default.** Local dev and any public deploy without the flag set behave exactly as before; no auth, no cookie, no redirects.

To enable it:

1. Mint a token — anything opaque works, e.g.:

   ```bash
   echo "tok_$(uuidgen | tr -d - | tr '[:upper:]' '[:lower:]')"
   ```

2. Set two env vars (in `.env` or your deployment's config):

   ```
   RULEBOOK_DEMO_MODE=true
   RULEBOOK_INVITE_TOKENS={"tok_abc123def456": "eric-test"}
   ```

   The value is a JSON `{token: recipient-label}` map — the label is only used for logging.

3. Share the invite link:

   ```
   https://your-deploy.example.com/?token=tok_abc123def456
   ```

   First visit exchanges the token for a `guest_session` cookie (httpOnly, Secure, SameSite=Lax, 30-day lifetime) and redirects to a clean URL. Every subsequent request authenticates via the cookie. Unknown tokens and cookie-less visits get a minimal invite-only 401 page.

Revoking access means removing the entry from `RULEBOOK_INVITE_TOKENS` and redeploying — cookies aren't signed, so the middleware re-checks the allowlist on every request.

## What this project deliberately does _not_ do (yet)

Kept out of v1 so each is a clear next lesson rather than a bundled complexity:

- **Reranking.** Baseline top-_k_ cosine only. Add a cross-encoder rerank step when you can see the baseline fail.
- **Hybrid retrieval.** Dense-only. Adding BM25 helps for exact rule-number lookups.
- **Streaming answers.** The API returns a single JSON blob. Streaming is nicer UX but hides latency structure while you're learning.
- **Evaluation harness.** No automated eval. Build one after you have opinions about what "good" looks like.

## License

TBD.
